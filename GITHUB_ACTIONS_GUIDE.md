# 📱 GitHub Actions 自动构建APK 完整教程

## 🎯 什么是GitHub Actions？

GitHub Actions是GitHub提供的免费自动化服务。你只需要把代码推送到GitHub，它就会自动帮你构建APK，完全不需要电脑！

---

## 📋 准备工作

### 1. 注册GitHub账号
- 访问 https://github.com
- 点击 "Sign up" 注册账号
- 验证邮箱

### 2. 创建新仓库
1. 登录GitHub后，点击右上角 **+** 号 → **New repository**
2. 仓库名称填写：`mc-addon-generator`
3. 选择 **Public**（公开）
4. 勾选 **Add a README file**
5. 点击 **Create repository**

---

## 📤 上传你的代码

### 方法一：通过网页上传（最简单）

1. 进入你刚创建的仓库
2. 点击 **Add file** → **Upload files**
3. 将以下文件/文件夹拖入上传区域：
   - `src/` 文件夹
   - `public/` 文件夹
   - `index.html`
   - `package.json`
   - `tsconfig.json`
   - `tsconfig.node.json`
   - `vite.config.ts`
   - `tailwind.config.js`
   - `postcss.config.js`
   - `components.json`
   - `.github/workflows/build-apk.yml`（GitHub Actions配置文件）

4. 点击 **Commit changes**

### 方法二：使用Git命令行

```bash
# 克隆仓库到本地
git clone https://github.com/你的用户名/mc-addon-generator.git
cd mc-addon-generator

# 复制你的项目文件到这里
# ... 复制文件 ...

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 推送到GitHub
git push origin main
```

---

## 🚀 触发自动构建

上传代码后，GitHub Actions会自动开始构建：

1. 进入你的仓库
2. 点击上方的 **Actions** 标签
3. 你会看到 "Build Android APK" 工作流正在运行
4. 等待约5-10分钟（第一次构建较慢）

---

## 📥 下载APK文件

构建完成后：

1. 点击 **Actions** 标签
2. 点击最新的工作流运行记录
3. 滚动到页面底部的 **Artifacts** 区域
4. 点击 **MC-Addon-Generator-Debug** 下载APK

![下载示意图](https://docs.github.com/assets/images/help/actions/artifacts-download.png)

---

## 🔧 常见问题

### Q1: 构建失败了怎么办？

**查看错误日志：**
1. 点击失败的运行记录
2. 点击左侧的构建步骤
3. 查看红色❌标记的步骤错误信息

**常见错误及解决：**

| 错误 | 解决方法 |
|------|----------|
| `package-lock.json` 不存在 | 运行 `npm install` 生成本地，然后上传 |
| 构建超时 | 重新运行工作流 |
| 内存不足 | 检查是否有大文件（如视频），删除后重试 |

### Q2: 如何重新构建？

**方法一：** 修改任意文件并推送

**方法二：** 手动触发
1. 进入 **Actions** 标签
2. 点击 **Build Android APK**
3. 点击 **Run workflow** → **Run workflow**

### Q3: 构建的APK安装不了？

- 确保开启了 "允许安装未知来源应用"
- 下载的是Debug版本（不需要签名）
- Release版本需要签名才能安装

---

## 🔄 持续更新

每次你更新代码并推送到GitHub，Actions都会自动构建新的APK：

```bash
# 修改代码后
git add .
git commit -m "更新功能"
git push origin main
```

然后等待几分钟，新的APK就准备好了！

---

## 💡 高级配置

### 自动发布到GitHub Releases

如果你想自动发布正式版本，可以在 `.github/workflows/build-apk.yml` 末尾添加：

```yaml
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.run_number }}
        release_name: MC Addon Generator v${{ github.run_number }}
        draft: false
        prerelease: false
```

---

## 📞 需要帮助？

如果在使用过程中遇到问题：

1. 查看Actions日志获取详细错误信息
2. 检查所有必要文件是否已上传
3. 确保package.json中的scripts配置正确

---

## ✅ 检查清单

上传代码前，确认有以下文件：

- [ ] `src/` 文件夹（包含所有源代码）
- [ ] `public/` 文件夹（静态资源）
- [ ] `index.html`
- [ ] `package.json`
- [ ] `vite.config.ts`
- [ ] `tsconfig.json`
- [ ] `tailwind.config.js`
- [ ] `.github/workflows/build-apk.yml`

---

## 🎉 完成！

按照以上步骤，你就可以在手机上获取自动构建的APK了！

每次更新代码 → 推送到GitHub → 等待5分钟 → 下载最新APK

完全不需要电脑！🚀
